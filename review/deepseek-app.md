I'll read the review prompt file first.
SAFETY | The crisis response only appears when an optional free-text line matches a keyword, so a person in acute distress who picks a mild idiom or types nothing never sees it | Make the crisis card and one-tap helplines permanently reachable from every screen, with keywords as an addition not the mechanism | HIGH
SAFETY | A machine "most days this week were heavy" nudge can crystallize a negative self-view or read as a diagnosis when the person has no local help to reach | Frame it as a neutral observation always paired with a concrete offline action (a trusted person, a health post), never an inference about the person | HIGH
SAFETY | The crisis card carries only tap-to-call and "asking is safe," omitting the site's own suicide-page guidance to not leave the person alone and to remove means | Add "stay with the person, do not leave them alone" and "keep pesticides and means away for now" to the crisis card | HIGH
PRIVACY | Android Auto Backup uploads app data to Google Drive by default, silently breaking "data never leaves the phone" | Disable android:allowBackup and exclude the SQLite directory in fullBackupContent, then prove it with a backup test, not a note | HIGH
SAFETY | Phones are explicitly shared in the target context, and the log, free text above all, is readable by any family member who opens the app with no lock or hide option | Add an optional passcode or "hide my entries" to the log, or at minimum warn on first run that entries are visible to others | HIGH
PRODUCT | The Learn section is sourced from atlas chapters that are DSM-5 diagnostic criteria, which would turn a warm app into a clinical diagnosis tool | Emit only the plain-words and skill boxes into app-content.json and exclude diagnostic criteria from the app build | HIGH
PRODUCT | v1 promises "know where help is" but ships only national helplines, which the site's own ledger flags as often unreachable, with no local health post or OCMC list | Bundle a static region-tagged list of health posts and OCMCs offline so "where help is" is actually true without a live directory | HIGH
PRIVACY | The Learn content inherits external links (WHO, YouTube, Springer), so tapping one leaks that the user is in a mental-health app | Strip external links from in-app content or require an explicit confirm before opening the browser, and keep a zero-network launch check in CI | MEDIUM
SAFETY | The crisis card relies on helplines that may not answer in a remote district, leaving a distressed person with no in-moment action | Include an offline two-minute grounding skill and a "call someone you trust" step on the crisis card, not only a number | MEDIUM
PRIVACY | SQLite logs are plaintext on disk, so an unlocked or lost phone exposes entries despite the "data never leaves the phone" framing | Adopt SQLCipher at build time or state plainly that entries are not encrypted and the phone should be kept locked | MEDIUM
TECH | Expo SDK 54 minimum is Android 7, but many cheap district phones still run Android 5/6, excluding part of the stated target audience | Check the actual device distribution of target users before locking the SDK/minimum version | MEDIUM
SAFETY | The keyword list is "in both scripts," but Nepali users often write distress in Romanized form or idiom (aatyahat, man dukhyo, "sakiyo"), so real crises will be missed | Add Romanized spellings and common idioms to the list and treat detection as a supplement to an always-available crisis path | MEDIUM
PRODUCT | The loop maps a fixed set of six idioms to one canned response, which will feel dismissive when a feeling does not fit the menu | Allow multiple selection and write responses that acknowledge not-fitting, with free text carrying the real weight | MEDIUM
PRODUCT | There is no plan to test with actual target users (low literacy, shared phones, offline) before launch, so the idiom list and reading level are assumptions | Run a small in-field pilot in one remote district before the store launch and adjust copy and flow from it | MEDIUM
PRODUCT | Low-literacy Nepali-first users may not read the response or skill at all, and no audio is planned for v1 | Add a short recorded Nepali voice-over for the two-minute skills, or explicitly state the literacy assumption and defer | MEDIUM
SAFETY | There is no way to delete or clear an entry, so on a shared phone a regretted entry stays visible forever | Allow deleting individual entries and clearing the whole log | MEDIUM
STORE | The owner's personal developer name becomes public on both stores, and no entity or identity decision is made | Decide developer identity (individual vs registered entity) before creating the store accounts | MEDIUM
STORE | The app's "skill to try" content could be read by reviewers as a treatment claim, and the site's "coping skill, not treatment" disclaimer is not ported into the plan | Carry the exact disclaimer line and a not-a-medical-device statement into every skill and the store listing | MEDIUM
STORE | Bundled helplines can go stale, and a wrong number in a crisis is worse than none | Re-verify every helpline against the site's fact ledger at each release and show a "last verified" date on the Help screen | MEDIUM
PRIVACY | "Data Not Collected" can still be broken by expo-updates or EAS Insights left in a template, and the research only flags this as something to audit | Pin a bare-minimum Expo config with an automated check that no analytics-capable module is present in release builds | MEDIUM
TECH | The research claims the "JS ecosystem is shared with the site," but the site is a Python build with vanilla JS, so the only real shared asset is the content pipeline, not code | Scope the "shared" claim to the content-to-JSON pipeline and do not count on code reuse between the two | LOW
TECH | expo-router is heavier than a three-screen app needs and adds a dependency a small team must learn | Use a minimal navigator and drop expo-router unless deep-linking is genuinely needed in v1 | LOW
PRODUCT | The pattern nudge has no time or count threshold, so it can fire after trivial or few logs and read as noise or surveillance | Define a minimum number of logs and a time window before the pattern line can appear | LOW
STORE | The CC BY-NC-SA content license is inherited from the site but no attribution requirement is specified for the app or store listing | State the license and attribution in the app's About screen and store listing | LOW
VERDICT: Proceed with changes: the offline-first concept and Expo choice are sound, but the crisis path, shared-phone privacy, and the Learn section pulling DSM-5 criteria into the app must be fixed before any launch, and the "data never leaves the phone" claim needs the Android-backup and no-network checks proven, not asserted.
