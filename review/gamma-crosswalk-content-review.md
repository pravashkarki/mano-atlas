# reviewer-gamma crosswalk content review

DISAGREE. The rebuild gets the official figures, bilingual table structure, status vocabulary, on-site-only links, and quiz answer keys right, but it still needs the approved numbering note, more accurate module-to-page mapping, and removal of three promises that every syllabus topic is available or searchable.

1. **must-fix**, `content/crosswalk.html:112-113`: Exact text: `"Everything the PDF examines is findable here, by topic or by search."` The matching Nepali sentence makes the same claim. This is false because the same page marks Facilitation Skill as `"not yet on the site"`, while `review/curriculum-coverage.md` records many missing tasks. Suggested fix: say that the map shows what is covered or partly covered, makes the remaining gaps visible, and helps readers search material already on the site; add matching Nepali wording.
2. **must-fix**, `keypoints/crosswalk.html:1`: Exact text: `"Every module and sub-module of the PSC course maps to a page here"` and `"you will find any topic."` Facilitation Skill has no atlas page, and the audit contains many missing curriculum tasks. Suggested fix: say that every module and sub-module is listed with either its atlas page or an honest gap marker, and that search can help readers find topics already on the site; add matching Nepali wording.
3. **must-fix**, `quizzes/crosswalk.html:1`: The third answer's feedback says, `"search finds any topic either way."` The Nepali feedback repeats the claim. This teaches students something the page's own gap row disproves. Suggested fix: say that the map shows both covered topics and gaps, and that search can help find material already on the site; add matching Nepali wording.
4. **must-fix**, `content/crosswalk.html:107-108`: The current note, `"Hours are as printed in the curriculum; a few sub-module rows do not add exactly to the module total in the source document."`, addresses arithmetic only. The approved plan also requires a one-line bilingual note about the PDF's two module-numbering systems. Suggested fix: add, `"The source PDF uses two module numberings in different sections. This site follows the numbering in its course-structure table."` Add matching Nepali wording beside the course table.
5. **must-fix**, `content/crosswalk.html:13,16,25,37,40,49`: The Mental Health row omits `{{page:nepal}}`, although the audit places the Nepal service timeline and current service material there. The Special Issues row omits `{{page:gbv}}`, although Gender and GBV is a named task under that sub-module. The Basics of Counselling row includes `{{page:roots}}`, but the audit says that page covers psychology history, not counselling history. Suggested fix: add Nepal to Mental Health, add GBV to Special Issues, and replace Roots in Basics of Counselling with Nepal; mirror all three changes with the Nepali page tokens.
6. **should-fix**, `content/crosswalk.html:9-56,64-106`: The only dedicated hours table is the expander titled `"Where the hours sit, sub-module by sub-module"`, and it has no Marks column. Module hours and marks are embedded in map header rows instead of the approved Module / Hours / Marks table. Suggested fix: add a compact bilingual four-row course table using the official module totals, and keep the sub-module hours in the expander.
7. **should-fix**, `content/crosswalk.html:12,23,26,36,47,50`: Wellbeing, Approaches, and Communication Skills are labelled `"covered here"`, but the audit marks several named tasks within each as PARTLY rather than taught at curriculum depth. Suggested fix: change these three row statuses to `"partly"` and `"आंशिक"` so the labels match the page's own definition of Partly and the audit evidence.
