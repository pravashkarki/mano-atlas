MARKDOWN CONTRACT (1) | "paired block markers, e.g. :::en ... ::: :::ne ... :::" | DISAGREE | The :::en/:::ne convention is not valid CommonMark; it creates raw HTML blocks that most Markdown parsers will pass through verbatim or strip, breaking the build's resolve_refs and gate_sources which operate on the rendered HTML. Use a frontmatter-driven approach: store English prose in the body, Nepali in a parallel `ne_body` frontmatter field (or separate file), and have build.py pair them at render time — this keeps authoring simple, gates working, and avoids parser ambiguity.

MARKDOWN CONTRACT (1) | "fallback: keep HTML fragments for bilingual pages and restrict Markdown to English-only phase-1 pages" | AGREE-WITH-CHANGE | This fallback is the sound path: phase-1 pages are English-only Markdown (no pairing needed), bilingual pages stay as HTML fragments until phase 2. The plan should make this the primary strategy, not a fallback, and define the migration trigger (native review complete) explicitly in frontmatter.

MARKDOWN CONTRACT (1) | "frontmatter: keypoints (list of three, en and ne), quiz (three questions, options, feedback, en and ne)" | RISK | Duplicating keypoints and quiz in frontmatter diverges from the existing `keypoints/` and `quizzes/` file-per-page convention that the build validates. Keep keypoints/quiz as separate files (now `.md` if you wish) so gate_sources and the "three items" rule still apply; frontmatter should only hold metadata (slug, titles, language flag, glossary refs).

BILINGUAL PHASE ORDER (2) | "hiding is done at build, not by CSS: no hidden Nepali in the DOM, no orphan search hits on invisible text" | AGREE-WITH-CHANGE | Build-time suppression is correct — it eliminates orphan search-index entries, keeps the language switcher honest, and avoids CSS/JS toggle debt. However, the plan must specify: (a) the single build constant name (e.g. `PHASE1_ENGLISH_ONLY = True`), (b) that `lang.js` receives this flag and removes the NE button entirely (not just hides it), (c) that search-index generation skips `class="ne"` strings when the flag is on.

BILINGUAL PHASE ORDER (2) | "Existing Nepali stays in the source, untouched; it returns per page when its frontmatter flag flips (phase 2)" | RISK | The existing 29 HTML pages have Nepali baked into every fragment. The plan doesn't say how those pages are treated in phase 1. They must either: (a) be exempt and keep their toggle (since they already shipped), or (b) have their Nepali suppressed too. The plan must decide and document which, else the "site-wide" claim is false.

APP BUNDLE (3) | "holding per-slug reading content: slug, titles (en, ne), group, keypoints, quiz, glossary terms, cross-references. Not the full prose in v1 unless the app asks; sections and plain-words summaries come first" | MISSING | The Mano app's core loop needs "one short kind paragraph in second person written for the app (not the atlas's counselor voice)" per feeling state — this is NOT the atlas prose. The bundle must include: (a) a mapping from atlas slugs → app feeling-state keys, (b) the app's own response copy (or a clear signal that it lives elsewhere), (c) helplines with last-verified dates (already in SITE). Without this, the bundle is a catalog, not a consumable content source.

APP BUNDLE (3) | "versioned content-bundle.json (build date + content hash)" | RISK | Content hash of what? If it hashes only the bundle JSON, it misses changes to the source HTML/Markdown that don't alter the extracted fields. Hash the source files (content/, quizzes/, keypoints/) or the git commit SHA so the app can verify freshness against the repo.

GATES AND BUILD (4) | "build.py learns to parse both: existing HTML fragments stay untouched; Markdown pages generate the page and an entry in a versioned content-bundle.json" | DISAGREE | The plan assumes Markdown parsing can reuse `resolve_refs`, `stamp_badge`, `gate_sources` unchanged. It cannot: those functions expect HTML with `class="en/ne"` spans, `secbadge` spans, and cross-ref tokens. Markdown pages need a pre-render step that produces the exact HTML fragment shape the gates expect, or the gates must be rewritten to work on an intermediate AST. This is a silent divergence that will produce inconsistent pages.

GATES AND BUILD (4) | "gate_sources() scans content/, quizzes/, keypoints/ for em-dashes" | RISK | If Markdown pages are added, `gate_sources` must also scan `content/*.md` (and `quizzes/*.md`, `keypoints/*.md` if those migrate). The plan doesn't mention this; the gate will silently stop applying to new pages.

GATES AND BUILD (4) | "no hand-written chapter numbers" enforced by `STALE_RE` in `resolve_refs` | MISSING | The plan doesn't address how chapter numbers work for Markdown pages. Since numbers come from PAGES position, Markdown pages inserted into PAGES will renumber correctly — but only if the author doesn't write `{{n:slug}}` in Markdown prose expecting a different number. The gate must run on the rendered HTML, not the Markdown source.

VOICE ADOPTION (5) | "docs/VOICE-TONE.md is the copy standard. The content pair review and the readability checks reference it." | MISSING | The plan adopts the voice guide but doesn't specify: (a) how the readability plan in `review/readability-plan.md` (if it exists) integrates — e.g., paragraph-length gate (~70 words), subject-verb-early check, banned-word list; (b) that the pair-review prompts for phase-1 English pages must include the voice guide's "What the voice refuses" section as a checklist; (c) that the "no classroom role-play" rule means the "A skill to try" boxes must be rewritten in second-person "you" language, not third-person counselor instructions.

VOICE ADOPTION (5) | "Phase-1 English-only pages are written against it directly." | RISK | The voice guide says "Nepali uses its own idioms and its own rhythm" and "Carry the voice across, not the words letter by letter." Phase-1 pages written English-first with no Nepali counterpart risk baking in English idioms that don't translate. The plan needs a "translation readiness" note in frontmatter or a parallel review step before phase 2.

CONTRACT-VERDICT: Require Markdown pages to be English-only in phase 1 with no bilingual markers; store Nepali in a parallel `content/<slug>.ne.md` file (or frontmatter `ne_body`) only when phase 2 flag flips; render both through a single HTML-fragment emitter so all existing gates apply unchanged.

BUNDLE-VERDICT: Add a `feeling_state_map` array linking atlas slugs to app feeling-state keys, and include the app's own response paragraphs (or a `response_ref` pointing to the app's separate copy source) plus helplines with `last_verified` dates — the bundle must be consumable by the app's core loop without a second authoring pass.

PHASE-VERDICT: Yes, build-time toggle hide is sound — it eliminates orphan DOM, search-index pollution, and toggle UI debt; the single build flag must also control `lang.js` button removal and search-index language filtering.
