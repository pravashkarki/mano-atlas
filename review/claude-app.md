SAFETY | The atlas text is written for counselors ("your client", risk ladders, "do not leave the person alone"), so "responses drawn from the atlas" via a build step will put clinician-voice and assessment language in front of a distressed lay reader | Write the app copy as a separate, second-person source (own file, same fact ledger), and drop the "site and app can never drift" pipeline claim; only helplines and facts are shared | HIGH
SAFETY | A keyword trigger on free text is unreliable in Nepali (figurative "मरिसक्यो", "मर्न आँटेको", romanized "marna man lagcha", typos), giving both misses and false alarms | Make "म सुरक्षित छैन / I do not feel safe" a first-class state chip that opens the crisis card, keep the keyword scan as a backup covering Devanagari and Latin transliterations, and consider cutting free text from v1 | HIGH
SAFETY | The crisis card has no means-safety line, although the site itself says pesticide storage is "the single conversation that saves most lives" in Nepal | Add one first-person line to the crisis card: put pesticides and medicines out of reach tonight, ask someone you trust to hold them | HIGH
SAFETY | "Asking is safe" is a myth-buster written for the person doing the asking; the app user is the person in danger | Replace with "telling someone is safe, and it helps" plus "if you are alone right now, call, or go to the nearest person you trust" | HIGH
SAFETY | On a shared phone the History calendar and the pattern line expose a week of feelings to a spouse, in-laws or parents, which is exactly the GBV and young-bride risk group the site names | Add an optional PIN or biometric app lock, a one-tap "delete everything", blank the recent-apps thumbnail (FLAG_SECURE / blur on background), and keep notification previews generic | HIGH
SAFETY | Tap-to-call assumes signal and a helpline that answers; remote districts often have neither, and a card that only lists numbers can land as "no help exists" | Show opening hours for 1166 and 1660 012 1600, a plain fallback when nobody answers, and offline pathways: FCHV, health post, OCMC, a trusted person; verify hours before launch | HIGH
PRIVACY | Android backs up app data to Google by default; Expo does not disable it, and Android 12+ has separate cloud and device-to-device transfer rules | Set android.allowBackup=false and dataExtractionRules excluding the database; verify with `adb shell bmgr backupnow` and an `adb backup` attempt; on iOS set NSFileProtectionComplete and the isExcludedFromBackup attribute on the SQLite directory | HIGH
PRIVACY | React Native Android templates ship the INTERNET permission, so "no network permission" is not true unless removed | Strip INTERNET from the Android manifest via app config (tel: links do not need it), then confirm in the release build's merged manifest and with a proxy that zero requests occur; treat "open the site" as a later feature that reintroduces it | HIGH
TECH | Long-term maintainability is the weakest point: Expo SDKs go unsupported yearly, Play raises target API every year, and the owner's skills are Python and static HTML; a stalled app is worse than none | Before committing, prototype the same loop as a PWA plus Trusted Web Activity for Play (existing HTML/CSS, offline via service worker, IndexedDB); the plan dismisses PWA on iOS grounds that barely apply to Android-heavy remote Nepal | HIGH
STORE | Both stores are needed only if iOS matters; Apple health review, the developer account cost, and iCloud rules double the launch work for a tiny share of district phones | Ship Android first, iOS only when there is a reason; the plan never says who the iOS users are | MEDIUM
STORE | New personal Google Play accounts must run a 14-day closed test with 12+ testers and pass identity verification before production | Start the Play account and closed test now, decide the publishing identity (person or LastDoor), and budget the delay | MEDIUM
STORE | Data safety and the privacy policy must mention what the platforms collect regardless of the app (Play vitals, Apple opt-in crash logs) or the "we see nothing" claim is inaccurate | State it in one sentence in the policy; host the policy at a stable URL on the site in both languages before the Play declaration | MEDIUM
PRODUCT | The pattern nudge ("most days this week were heavy") after a flood labels a normal acute reaction as a problem, and PFA on the site says most reactions settle without a label | Make the nudge normalise first, then offer help; never show it right after a heavy log; make it dismissible for good; show it only with N logs by the same person (which a shared phone breaks) | MEDIUM
PRODUCT | The state list and idioms are unvalidated; getting "man" idioms wrong makes the app feel foreign on first tap | Draw states from validated Nepali idiom research (TPO Nepal / Kohrt's man, dimaag, saato work) and test with ten district users before copy freeze; name the Nepali reviewer now | MEDIUM
PRODUCT | Post-disaster alcohol use is common and the substance chapter is in scope, but no state covers it | Add one state for drinking more or feeling out of control, with a non-judging response | LOW
PRODUCT | Helplines are hard-coded in bundled content with no OTA; a number change means a store release with review lag | Accept it, but keep helplines in one JSON with a visible "checked on" date and a store-release checklist; do not add a network fetch to fix it | LOW
TECH | Encryption plan is vague: expo-sqlite has no SQLCipher, iOS default protection is only until first unlock, and "use the keystore" is not a design | Decide now: either op-sqlite with SQLCipher and a key in Keychain/Keystore, or state plainly that protection relies on the phone lock; either way the app lock is what protects against the real threat, which is household access | MEDIUM
TECH | 25 MB target is optimistic: a blank Expo release build is already near that before Mukta and SVGs | Measure the first build, ship AAB with per-ABI splits and Hermes, subset Mukta, and set a hard budget of 30 MB installed | LOW
TECH | Expo SDK 54 minimum Android version and cold-start on 1 to 2 GB Android Go phones are assumed, not tested | Verify the SDK's minSdk against the actual phones in the districts and measure cold start on a Go device before writing screens | MEDIUM
TECH | Free text plus keyword scanning means crisis entries persist in History and can resurface the crisis card when browsing old days | Never re-trigger the card from History; store the state, not the trigger | LOW
STORE | Suicide and self-harm content will push the age rating up (IARC / Apple), which can hide the app from the teenagers who most need it | Word the crisis card and listing to keep a 12+ rating; declare target audience 18+ on Play to stay out of Families policy while still allowing older teens | LOW
STORE | Nepal has the Individual Privacy Act 2075 and no mental-health app rules; the plan cites only US store policy | Add one line in the policy referencing the Privacy Act, and keep the "not a medical device, not a substitute for a doctor or counselor" disclaimer visible in-app in both languages, not only in the listing | LOW
PRODUCT | Reasons people delete: forced language, a heavy first screen, a reminder that reads as nagging, and a card that shouts | Default language from device locale with one-tap switch, keep the first screen to the state chips only, opt-in reminders with a soft preview, calm crisis colour per the site's crimson rule | LOW
VERDICT: Proceed with changes; the loop is sound but the plan needs a separate lay-voice copy source, a state-chip safety trigger with means safety, shared-phone protections, verified backup and permission controls, and a real PWA/TWA comparison before Expo is locked in.
