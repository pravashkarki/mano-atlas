# Planning files, reviews and the intake ledger are tracked (decided 2026-09-02) but kept off the
# Vercel deployment by .vercelignore. Only these stay out of git.
# verbatim text extracted from instructor decks: not ours to publish
review/sources/
# teacher-provided course PDFs are for PCS students only; the repo is public
drive-source/
# opencode run logs
review/*.err
_notes/
*.local.*

# CLAUDE.md is ignored globally on this machine; this repo tracks it on purpose
!CLAUDE.md

# OS / editor junk
.DS_Store
Thumbs.db
*.swp

# python
__pycache__/
*.pyc

# node (if ever added)
node_modules/
